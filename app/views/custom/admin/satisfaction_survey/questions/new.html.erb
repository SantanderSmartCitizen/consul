<%= back_link_to %>

<h2 class="margin-top">
  <% if @poll.present? %>
    <%= t("admin.satisfaction_surveys.questions.new.title", poll: @poll.name) %>
  <% end %>
</h2>

<div class="poll-question-form">
  <%= render "form", form_url: admin_satisfaction_survey_questions_path %>
</div>
