<% series = stats.winning_investments_by_execution_status %>

<% if series.present? %>
	<%= render "shared/stats/donut_chart", series: series, title: t("stats.by_execution_status"), exportable: exportable %>
<% end %>